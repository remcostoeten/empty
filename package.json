{
  "name": "selfhosted-analytics",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -ws",
    "lint": "npm run lint -ws",
    "test": "npm run test -ws"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
